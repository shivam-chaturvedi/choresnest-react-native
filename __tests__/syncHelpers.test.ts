/**
 * Legacy helper tests moved under __tests__/sync.
 * Keep this file as a pointer so old scripts don't fail silently.
 */
describe('legacy syncHelpers location', () => {
  test('sync helpers live in __tests__/sync', () => {
    expect(true).toBe(true);
  });
});
